# Progress và trạng thái triển khai implementation-plan

Ngày cập nhật: phiên này (nhánh `codex/implement-upgrade-plan`). Tài liệu này ánh xạ từng task của
[docs/plans/implementation-plan.md](../plans/implementation-plan.md) sang trạng thái + bằng chứng hiện tại.
Trạng thái dùng: DONE (acceptance + test xanh), PARTIAL (có code/test thật nhưng acceptance còn thiếu),
NOT_STARTED, NOT_RUN/BLOCKED (chỉ do thiếu môi trường live/cloud/model — theo quy tắc plan §2, không tự bịa evidence).

## Cổng nghiệm thu hiện tại

- Backend full suite (repo root): `.venv\Scripts\python.exe -m pytest backend/tests -q` → **700 passed**, 1 warning
  (SQLAlchemy FK-cycle sort, có sẵn từ baseline), exit 0.
- Frontend: `npm test -- --run` → **192 passed / 40 files**; `npm run build` (tsc + Vite) → PASS.
- **Browser E2E** (mới): `npx --no-install playwright test` → **7 passed** (4 spec) trên **Chrome hệ thống
  qua `channel: 'chrome'`** — xem "Thay đổi môi trường browser" bên dưới.
- Ruff các file thay đổi: PASS. Không chạy cloud trả phí/live/model download trong toàn bộ quá trình.

### Thay đổi môi trường browser (quan trọng — mở khoá nhiều mục NOT_RUN)

Ghi nhận trước đây ("không có Chromium/Chrome/Edge binary ⇒ mọi kiểm visual/E2E/responsive NOT_RUN") **không
còn đúng**. Probe trong phiên (script tạm, đã xoá):

```
PROBE_OK channel:chrome    text=xin chào version=152.0.7977.83
PROBE_OK executablePath    text=xin chào version=152.0.7977.83
PROBE_OK channel:msedge    text=xin chào version=152.0.4191.66
```

Playwright 1.54.2 (đã có trong devDependencies) chạy được Chrome/Edge hệ thống dù cache `ms-playwright`
không tồn tại. Vì vậy `frontend/playwright.config.ts` nay mặc định `use.channel = 'chrome'`; các mục
visual/E2E/responsive **đã chạy thật** thay vì NOT_RUN. Phần còn NOT_RUN chỉ là các mục cần môi trường live
thật (cloud có quyền, model VieNeu + license, corpus có quyền, reviewer/người nghe).


## Ma trận task → trạng thái → bằng chứng

| Task | Trạng thái | Bằng chứng (commit / test) |
|---|---|---|
| F01 Baseline/backup/restore | DONE | 39b9e93, f56dfc3; baseline.md PASS |
| F02 Typed contracts/lỗi | DONE | beb3066; contract tests |
| F03 Schema provenance/migration | DONE | af14155, 2e9f280; schema frozen/round-trip |
| S01 Keyring/credential lifecycle | DONE | e53b790…6963297 chain; security tests |
| S02 Chặn rò key/CSRF/loopback | DONE | close 2e5b91a (r8 PASS); 127+ security tests |
| S03 Quote/reservation guard | DONE | afb242c, 12a9189, ab3603d; budget/compliance tests |
| P01 Registry/descriptor capability | DONE | e53b790, 483f332; test_registry_catalog |
| P02 Discovery/catalog phân trang | DONE | 1dc1cdc, ae5f1f4, 4f95897, 6f78cdc |
| P03 Transport/SSE parser | DONE | d61c02b; test_transport |
| P04 Adapter Gemini native | DONE | 3c7fb21; full 564→… chạy trong round đó |
| P05 Adapter Qwen-MT native (wire theo docs 2026-09-09) | DONE | 3c7fb21; error matrix/language codes |
| P06 Adapter OpenRouter | DONE | 9153cd0 |
| C01 Style profile/prompt builder/native-MT builder | DONE | 9339fe6 |
| C02 Glossary scope/forbidden/evidence | DONE | bff39b7 |
| C03 Translation memory exact | DONE | 69181bf |
| C04 Characters & directed addressing | DONE | ef22114 |
| C05 Summary approval + context engine | DONE | 6ca8eca |
| C06 QA/edit/approve/repair + segment coverage guard | DONE | 7de976e |
| J01 Worker/handler/checkpoint | PARTIAL | rounds 1–4: 4b7821b, 310a6ff, 07ea083 + round 4 (harness thật) — plan seam, batch child plan, real TRANSLATE handler, crash-before-provider-result & crash-after-commit chạy qua handler sản xuất (recovery không nhân đôi segment, lần chạy lại phục vụ từ cache nên **không gọi provider lần hai**). **Round 5 (78b85ac — task 63)**: đã nối **REVIEW** và **SUMMARIZE** handler theo đúng khuôn TRANSLATE (`to_thread` → `require_plan` → validate chapter/revision/profile → nhánh fake offline vs cloud), đăng ký đủ trong `build_default_handlers()` (TRANSLATE/SYNTHESIZE/REVIEW/SUMMARIZE; 6 kind còn lại vẫn `_unconfigured_recovery_handler`). Nhánh fake REVIEW chạy `deterministic-qa-v1` + locked glossary rules trên run REVIEW hiện tại và ghi `QaIssue` OPEN (key theo segment+rule nên **idempotent**); nhánh fake SUMMARIZE tạo recap extractive offline thành `story_memory_entries` **CANDIDATE** (chỉ promote được khi approve có evidence). Nhánh cloud **fail rõ ràng** với mã không-retryable (`REVIEW_CLOUD_PROVIDER_NOT_WIRED`, `SUMMARIZE_REQUIRES_CLOUD_PROVIDER`) chứ **không** giả thành công — 15 test mới (`pytest backend/tests/jobs -q` → 110 passed). Để SUMMARIZE có thể được worker nhận, `JobKind.SUMMARIZE` được thêm kèm migration **0018** (additive: rebuild `jobs` để mở rộng whitelist `ck_jobs_kind_enum`). Đã kiểm độc lập: upgrade thêm SUMMARIZE, `BOGUS` vẫn bị chặn (whitelist nghiêm ngặt), downgrade bỏ SUMMARIZE, re-upgrade lại được, index giữ nguyên; `backend/tests/db -q` → 23 passed. **Còn**: REPAIR_TRANSLATION handler (cloud) và UI E2E cho review/summarize — **NOT_RUN/BLOCKED** vì cần credential/consent/budget cloud |
| J02 Retry/cancel/breaker persist | DONE | 82ef1da, f4f0265, cfdc065 — persisted breaker + half-open + dispatch gate + HTTP retry classification (429 Retry-After, 401 no loop, billingUnknown no resend); cancel p95 timing thuộc G-PERF (V01) |
| J03 Projection/feed/diagnostics | DONE | EventLog append-only + cursor SSE + sync dedupe (d577b18, deb91f3); retention purge theo cutoff + rebuild parity (22b136c); emit cùng transaction tại job terminal & approve (round 3); structured log model/latency/cost redacted |
| J04 Draft streaming resumable | PARTIAL | Engine + snapshot + SSE feed + adapter delta source (`stream_translate`) đã xong & test. Round 4: worker ghi delta provider vào `workspace_drafts` (U04) qua `DraftDeltaSinkFactory` bind session → feed `GET /api/jobs/{id}/draft[/stream]` phục vụ draft overlay (offset theo segment, dedupe/gap, `draftRevision`, không bao giờ approvable) — 5 test. Còn: hiển thị delta **trong lúc** job chạy (SQLite một-writer: draft commit cùng transaction của run) |
| U03 Phân trang thư viện và import review | PARTIAL | Round 1: backend `ProjectQueries` + `GET /api/projects` — cursor ký HMAC (`LIBRARY_CURSOR_INVALID` khi giả), `limit` cap **100** (mặc định 20), filter `q` server-side, page metadata (`total/count/hasMore/nextCursor/mountedChaptersPerProject`); mỗi project chỉ mount **30 chapter summary** (window function) + `chapterCount` chính xác qua GROUP BY ⇒ dự án 120 chương không kéo chapter/không lộ source text vào payload, và số statement mỗi trang **hằng số** (test đếm `SELECT` ≤ 6) — 5 test. Import review: thêm 4 test cho `build_import_candidates` (DUPLICATE_ORDINAL gắn cho **mọi** section trùng kèm `sourcePath`, ORDINAL_MISSING/EMPTY_CHAPTER nêu đúng file, chương 1200 vẫn nhận, text giữ nguyên khi confirm). Frontend: thư viện dùng `page` (hiện `n/total`), nút **Tải thêm** theo `nextCursor`, ô tìm kiếm gửi `q`, và vẫn chạy khi backend cũ không có `page` — 3 test. **Round 2 (171d2d6 — browser E2E, task 56)**: `frontend/e2e/import-preview.spec.ts` chạy trên Chrome hệ thống với **thư mục thật trên đĩa** (2 file cùng số chương 1 + 1 file chương 2): route preview thật trả 3 candidate, **cả hai file trùng đều mang cảnh báo `DUPLICATE_ORDINAL` và nêu đúng `sourcePath` của mình**, file sạch không có cảnh báo, chữ Việt có dấu không bị mojibake, và sau khi xác nhận thì **3 file ⇒ 2 chương** (`_get_or_create_chapter` khoá theo (project, ordinal) nên file trùng thành revision mới của cùng chương — đúng lý do phải cảnh báo TRƯỚC khi xác nhận). Spec này **phát hiện một lỗi thật ở shell**: `Shell` gọi `useMatch(a) ?? useMatch(b)` nên số lượng hook đổi khi đi từ route project sang route chapter ⇒ React throw giữa render (“Cannot read properties of undefined (reading 'length')” trong `areHookInputsEqual`), làm sập cả app sau khi nhập xong. Đã sửa bằng cách gọi cả hai hook vô điều kiện. ⇒ **U03 coi như DONE (code + browser)** |
| U04 Draft API và optimistic concurrency | DONE (code) | R1 backend: bảng `workspace_drafts` (migration 0015, UNIQUE project/chapter/base revision) + compare-and-swap (`DRAFT_REVISION_CONFLICT`), validate segment/size cap, không đụng approved, API `GET/PUT /api/chapters/{id}/draft` (200/409/400) — 6 test. R2 seam: `append_draft_delta` (duplicate/gap/`commit=False`) dùng bởi worker (J04 R4) — 5 test. R3 editor: `useChapterDraft` + `DraftControls` khôi phục nháp theo base revision, gửi `expectedRevision`, 409 giữ nguyên bản đang sửa + diff theo segment + chọn ghi đè/dùng bản server, retry idempotent khi nội dung trùng — 9 test |
| U01 Design system/tokens/primitives | DONE (code) · browser audit PASS | 84341ae, 5f25a46, 10c0037, 4d4aa90, 53d4377; 24 shared/ui tests. **Browser audit nay đã chạy thật** (task 45 / commit 149c729): `frontend/e2e/visual-a11y.spec.ts` trên Chrome hệ thống — reflow 320/390/1024/1366 (không overflow ngang), WCAG 1.4.4 (text 200% ở 1280/1024: không overflow, không cắt chữ), focus bàn phím thật, chữ Việt/CJK không mojibake. Audit này **phát hiện lỗi thật**: cột grid `auto` bị chốt bởi max-content của `<select>`/header ⇒ cả app cuộn ngang ở 320/390px; đã sửa bằng `minmax(0,1fr)` + `minWidth: 0` + `flexWrap` cho nav. Contrast ≥4.5:1 / focus ≥3:1 vẫn là assert cấp unit (`tokens-contrast.test.ts`); screen-reader script vẫn NOT_RUN |
| U05 Tabs, dock và lưu layout | PARTIAL | Round 1: `workspaceLayout.ts` — reducer thuần (8 tab/pane; mở/đóng/reorder/split/dock/undock/reset), tab thứ 9 chỉ evict tab **sạch** (dirty → `TAB_LIMIT_DIRTY`), đóng tab dirty bị chặn (`TAB_DIRTY`) trừ `force`, `undock` từ chối khi tràn (`UNDOCK_OVERFLOW`), guard cross-project (`PROJECT_MISMATCH`), serialize/parse có version + sanitize — 12 test. Round 2: bảng `workspace_layouts` (migration 0016, UNIQUE project), service `load_layout`/`save_layout` (CAS `LAYOUT_REVISION_CONFLICT`, sanitize bỏ tab project khác + cap 8 tab/pane, version lạ → `migrated`), API `GET/PUT /api/projects/{id}/workspace-layout` (200/409/400/422/404) — 9 test; client `useWorkspaceLayout` (khôi phục theo project, `migrated` → layout mặc định, dispatch cục bộ + lưu CAS, 409 giữ layout local + reload) — 6 test. Round 3: UI thật trong shell — `WorkspaceTabs` (role=tablist/tab đúng ARIA, điều hướng bàn phím ←/→/Home/End/Enter/Delete, roving tabindex, đóng tab dirty bị chặn + `TAB_DIRTY` + nút "Đóng và bỏ thay đổi", tab đã đóng không tự mở lại khi đổi route, dock/undock khung phụ, lưu layout + báo 409) — 7 test; `workspaceRoutes.ts` (map route → tab: editor/QA/inspector/job/preview + chỉ route có project UUID mới persist) — 4 test; action `sync` idempotent trong reducer — 2 test; Shell gắn `WorkspaceTabs` theo route. **Round 4 (149c729)**: kiểm responsive 320/390/1024/1366 **đã chạy thật** trên browser (`visual-a11y.spec.ts`) và phát hiện + sửa overflow ngang của panel/form (grid `minmax(0,1fr)`, `minWidth:0`, nav `flexWrap`). **Round 5 (8444c1b — browser E2E, task 51)**: `frontend/e2e/workspace-tabs.spec.ts` chạy trên Chrome hệ thống với server thật — mở tab theo điều hướng trong app, `←`/`→`/`Home`/`End`/`Enter` đúng WAI-ARIA, dock/undock khung phụ, **lưu layout rồi reload trang vẫn khôi phục 2 tab**, đóng tab đang mở điều hướng sang tab còn lại. Spec này **phát hiện 2 lỗi thật** và đã sửa: (1) tab khôi phục từ layout không có route definition nên bấm/đóng không điều hướng được (đóng tab đang mở rơi về `/`) — nay tab id cũng là đường dẫn; (2) effect roving-tabindex phụ thuộc `focusIndex` nên sau `←` DOM focus đã đổi nhưng state bị kéo về tab active, khiến `Enter` kế tiếp kích hoạt **sai tab** — đã sửa và thêm test hồi quy (8 test `WorkspaceTabs.test.tsx`). ⇒ **U05 coi như DONE (code + browser)**, chỉ còn screen-reader script |
| U07 Job UI và draft streaming | PARTIAL | Round 1: `useJobDraftStream` (snapshot `GET /api/jobs/{id}/draft` trước, SSE `/draft/stream?afterOffset=` với `draft`/`gap`/`terminal`, frame trùng theo offset bị bỏ, gap → thay bằng snapshot, terminal đóng stream nhưng giữ text, resync thủ công, hoạt động cả khi không có `EventSource`) + `JobDraftPanel` (read-only, badge trạng thái, offset, cảnh báo truncated, khẳng định nháp **không** dùng để duyệt) — 6 test; route `jobs/:jobId/draft` + map tab JOB. **Round 2**: `jobStore.ts` — **một** store/SSE subscription dùng chung, ref-count (consumer thứ hai không mở thêm connection; unmount hết → đóng; remount không nhân listener), buffer **cap 1.000** + cờ `truncated`, reconnect có backoff + **đọc lại snapshot trước khi resume** (bịt gap), bỏ qua event replay theo `sequenceId`, `retryableFailures()` chỉ trả job FAILED có mã retryable; `JobsList` (một dòng/job theo event mới nhất, nhãn `CANCEL_REQUESTED`, hủy chỉ hiện khi còn dừng được, thử lại chỉ cho lỗi retryable, link sang nháp) — 14 test; `JobProgress` dùng store chung + link nháp + nhãn hủy. **Round 3 (8f75eda — task 58)**: `BatchQueue` tự theo dõi đúng `jobIds` của batch qua JobEventStore **dùng chung** (chưa có batch thì không subscribe ⇒ màn tĩnh không mở stream), bảng tiến độ từng job (nhãn trạng thái tiếng Việt, current/total), **partial failure**: `role=alert` tổng hợp “N/M job thất bại” + errorCode, job non-retryable được đánh dấu “không thể tự thử lại”, nút “Thử lại tất cả lỗi retryable” chỉ retry theo `retryableFailures()` (billingUnknown không bao giờ tự retry), hủy từng job QUEUED/RUNNING + nhãn CANCEL_REQUESTED, tổng kết + “Dọn trạng thái” chỉ clear local — 11 test. **Còn: E2E SSE thật** và **BLOCKED**: backend chưa có `POST /api/jobs/{id}/retry|cancel` (chỉ có cho voice preview-jobs) nên hành động retry/cancel từ UI là NOT_RUN — component đã có props `onRetry/onCancel` để nối khi route thật tồn tại |
| U06 Editor song ngữ, QA, inspector | PARTIAL | Round 1: `BilingualEditor` — hàng song ngữ key theo **stable source segment id**; pane nguồn là text `aria-readonly` (không có textbox cho source, chỉ target sửa được); **chặn lưu khi IME đang composition** (`IME_COMPOSITION_ACTIVE`) rồi lưu sau `compositionend`; `Ctrl/Cmd+S` lưu đúng đoạn đang sửa kèm `expectedRunHash`; **QA inspector** lọc theo severity + bấm issue → reveal/scroll đúng đoạn (`data-revealed`, `scrollIntoView`); áp dụng đề xuất QA vào draft; **CRITICAL đang OPEN chặn Phê duyệt** (kèm đường bỏ qua có chủ đích `force: true`); 409 giữ nguyên text đang sửa + hiện mã lỗi — 7 test; route `/chapters/:id/editor` + map tab EDITOR. Còn: inspector glossary/character/context trace, diff/repair proposal đầy đủ (`RepairDiff` cần proposal từ job REPAIR), virtualize + route conflict tests mở rộng. **Round 2**: backend `context_trace.py` + `GET /api/chapters/{id}/translation/context-trace` — run gần nhất (hash glossary/memory đã ghi), glossary revision + **chỉ rule khoá trong phạm vi ordinal**, story memory **chỉ APPROVED** + hash, nhân vật active (aliases/status), cờ `stale` so hash run ↔ hash hiện tại (5 test); frontend `ContextInspector` (glossary/memory/nhân vật, cảnh báo stale, trạng thái rỗng, lỗi trace, và **ID thô chỉ nằm trong Drawer chi tiết**) — 5 test, gắn vào `BilingualEditor`. **Round 3 (fecfe76 — task 60)**: `POST /api/chapters/{id}/review/repair-preview` + `/repair-apply` — proposal **bất biến** (cùng input ⇒ cùng hash, id mới mỗi lần xem trước), preview **chỉ đọc** (route không commit; test đếm trước/sau), apply tạo **run REVIEW mới** và run cũ thành `SUPERSEDED` (không đè approved), conflict được đóng mã đầy đủ (`REPAIR_PROPOSAL_CONFLICT`/`HASH_MISMATCH`/`CONSUMED`/`NOT_FOUND`, và expectedHash không phải 64-hex cũng bị từ chối nên id hàng không thể lọt qua được kiểm tra hash) — 12 test mới; frontend: `BilingualEditor` có checkbox từng đoạn, nút “Xem đề xuất sửa” chỉ hiện khi ≥1 đoạn được chọn, gắn `RepairDiff` + hàng Chấp nhận/Bỏ qua (**chấp nhận chỉ sửa draft cục bộ**, không gọi server từng dòng), “Áp dụng đề xuất” gọi apply một lần với `proposalId`+`expectedProposalHash`, 409/400 hiện nguyên mã — 5 test mới (`pytest backend/tests/translation -q` → 111 passed). **NOT_RUN/BLOCKED**: repair bằng model cloud thật (J01 handler REPAIR) — chỉ có fake preview/apply; cần chuyển apply sang adapter được authorize khi J01 hoàn tất |
| U10 Storage, Appearance, Advanced | PARTIAL | Round 1: **Storage** — `BackupService.list_backups()` (checksum/integrity từng bản, bản hỏng hiện là `verified: false` chứ không crash), `retention_plan(count)` **chỉ là kế hoạch** (`applied: false`, `requiresConfirmation`, `pruneOnCreate` — đổi retention không xóa file nào), `restore_copy()` bắt buộc `confirmTarget` khớp đúng đường dẫn + chỉ restore vào data root mới (từ chối target trùng/overlap, verify backup trước khi ghi), API `GET /backups`, `GET/PUT /retention`, `POST /backups/{id}/restore-copy` (400 thiếu xác nhận, 409 target tồn tại/backup hỏng/lock) — 5 test. **Feature flags** — `feature_flags.py` fail-closed (unsafe/unknown → `FEATURE_FLAG_UNSAFE:<name>`/`FEATURE_FLAG_UNKNOWN:<name>`, flag unsafe không bật được qua API, `applied: false` vì rollout thuộc R01) + API `GET/PUT /api/settings/feature-flags` — 5 test. **Appearance** — `uiPreferences.ts` (chỉ theme/font/density/reduceMotion; khóa lạ và khóa kiểu content/secret bị loại và **không** hiển thị lại; version + migrate) + `AppearanceSettings` panel (lưu/khôi phục/về mặc định, preview chỉ hiện document đã lọc) — 11 test. **Kiểm đối chiếu phiên này:** hai mục "còn" trước đó thực ra đã có sẵn — Storage UI đầy đủ (`StorageSettings.tsx` dùng `CleanupPreview` + execute đúng `planId`/`snapshotHash`, `StorageSettings.test.tsx` 5 test) và Advanced/diagnostics export trong UI (`Diagnostics.tsx` có export ZIP redacted, mount ở cả `/settings/advanced` lẫn nhóm advanced của project, `ProjectSettingsRoutes.tsx`/`SettingsRoutes.tsx`; browser check qua spec U02 round 5 + task 45). ⇒ **U10 coi như DONE (code)**; mục duy nhất còn lại là feature-flag *persistence/rollout* vốn thuộc R01 theo thiết kế (flag fail-closed, `applied: false`), không phải code thiếu |
| U02 App shell & Settings bảy nhóm | PARTIAL → DONE (shell + route tests) | P1 cây `/settings` 7 nhóm (4 test); P2 `GlobalNav` ba khu vực (3 test); P3 nested `projects/:projectId/settings` lấy project từ URL, nhóm translation render 4 manager theo project (3 test); P4 **Storage** (`StorageSettings`: dung lượng, danh sách backup kèm trạng thái checksum, retention chỉ tạo **kế hoạch** `applied:false` không xoá gì, cleanup preview → execute đúng `planId`+`snapshotHash`, phục hồi bản sao chỉ bật khi gõ lại đúng thư mục và chỉ với backup verified, lỗi backend hiện nguyên mã) — 5 test; P5 **TTS** dùng catalog giọng cục bộ (`VoiceBrowser`) với ghi chú giọng chỉ khả dụng khi đã cài model/license. **Round 5 (commit 8ea773b, 5 test)**: `ProjectNav` — context project lấy từ URL (deep link + Back giữ project), breadcrumb, 3 area là link thật có `aria-current`; Shell render `ProjectNav` chỉ trên route project; các nhóm nested thay placeholder bằng nội dung thật (tts → `VoiceBrowser`, storage → `StorageSettings`, appearance → `AppearanceSettings`, advanced → `Diagnostics`), đoạn copy placeholder đã xoá. P6 test router chuyển sang `MemoryRouter` (data-router nav no-op trong jsdom: `RequestInit: Expected signal … to be an instance of AbortSignal` — lỗi thật do AbortSignal foreign-realm vs undici). Còn: screen-reader script full / responsive trên từng màn chi tiết |
| U09 Quản lý style/glossary/nhân vật/memory | PARTIAL | P1 `StyleManager` 4 test; P2 `GlossaryManager` 6 test; P3 `CharacterManager` (evidence gate, addressing có hướng) 6 test; P4 `MemoryManager` (candidate approve cần run+segment, reject, context chỉ APPROVED + hash) 5 test — cả 4 gắn Settings→Translation. **Round 2 (d1ee513 — task 59)**: `POST /api/projects/{id}/glossary/preview` — xem trước **chỉ đọc** (không tạo revision, không invalidate) dùng **cùng** hàm tính phạm vi với upsert nên preview và áp dụng không thể lệch nhau: `affectedSegments` (kèm excerpt), `invalidatedRuns`, `revisionPreviewSha256`, và warning `SCOPE_EMPTY`/`ORDINAL_OUT_OF_RANGE`/`TERM_CONFLICT`/`ALIAS_OVERLAP`; frontend `GlossaryManager` có nút “Xem phạm vi” hiện số đoạn bị ảnh hưởng + số bản dịch sẽ bị đánh stale **TRƯỚC** khi bấm Lưu, và không tự động lưu sau preview — 10 test backend + `npx vitest run src/features/glossary` (40 passed cùng translation). ⇒ **mục “preview stale-scope” của U09 đã xong**; còn nested project routes |
| U08 Provider/model và quality settings | PARTIAL | P1 `modelCatalog` 6 test; P2 `ProfileEditor` credential masked 5 test (Settings→Providers); P3 `QualityPlanPanel` (Balanced mặc định, Quality/Maximum cần opt-in, quote theo stage + tổng/expiry/warnings, **đổi model ⇒ quote hết hiệu lực**, thiếu consent thì chặn không gọi API, 403 → alert) 6 test. **Round 4 (commit 1a05f21): panel đã được gắn vào màn dịch thật** (`TranslationScreen`) khi đã nhập profile cloud — trước đó panel chưa từng render ở màn nào nên acceptance "quote/ceiling/consent từng stage" không thể chạm tới; integration test qua router thật xác nhận: không có profile ⇒ panel không mount, chọn Quality thiếu opt-in ⇒ hiện cảnh báo và **không** có POST quote nào. **Round 5 (b7c0f2c — browser E2E, task 61)**: `frontend/e2e/provider-credentials.spec.ts` trên Chrome hệ thống + server thật: provision profile qua API thật → `/settings/providers` → lưu credential (PUT thật) → assert ô rỗng/type=password/**không input nào còn secret** + nhãn đã cấu hình; **rotate** secret thứ hai thành công; bắt mọi request của page — không origin lạ, secret chỉ nằm trong body `PUT /api/cloud-profiles/{id}/credential` (không query string); **localStorage + sessionStorage đọc toàn bộ ở 3 thời điểm đều không chứa secret**; xoá key ⇒ `secretConfigured=false`. Spec này **phát hiện một bug thật nghiêm trọng của `shared/api.ts`**: helper chỉ set `Content-Type: application/json` khi body là object, còn mọi call site truyền `JSON.stringify(...)` (string) ⇒ browser gửi `text/plain` ⇒ FastAPI trả **422 INVALID_REQUEST** ⇒ **mọi thao tác lưu từ UI đều thất bại** (credential, profile, glossary, character, memory, style, quality panel). Đã sửa tại commit `8611203`: body string đã stringify vẫn được set JSON, **không đè** `Content-Type` do caller chỉ định, và `FormData` vẫn để browser tự thêm boundary — kèm **6 test hồi quy mới** (`src/shared/api.test.ts`). Trước khi có spec này, unit test không bắt được vì mock fetch không kiểm tra header. Còn: quote ceiling E2E (BLOCKED cần budget live) |
| A01 VieNeu manifest/bridge/catalog | DONE (code) | 89cc0d1 (26 local-tts/speech tests); **live probe/playback NOT_RUN** (chưa cài model/license) |
| A05 Artifact Range và review audio | DONE (code) · **browser E2E PASS** | Backend + frontend trong `bf92ea8` (task 47 + task 50). Backend: route `GET/HEAD /api/chapters/{id}/audio/artifacts/{artifactId}/content` — 200/206/416/304 + `Accept-Ranges`/`ETag`/`If-Range`, nhiều khoảng ⇒ 200 toàn file (không multipart), path confinement qua `ArtifactStore.resolve()` (leo thư mục / absolute / chapter khác ⇒ 404, không bao giờ 500), **42 test mới** (`pytest backend/tests/audio -q` → 47 passed, exit 0). Frontend: `ArtifactPlayer.tsx` — thẻ `<audio controls preload=metadata>` trỏ thẳng URL artifact (**không Blob toàn master**), Space/K phát–dừng, ←/→ tua 5 giây, PageUp/PageDown nhảy đoạn, Home/End, thanh tua có nhãn, cảnh báo `AUDIO_PLAYBACK_FAILED`/`AUDIO_SOURCE_UNAVAILABLE`, và cảnh báo “đã nạp bản master mới” khi sha đổi — 15 test; `AudioScreen` nạp master khi deep-link và **chặn phê duyệt khi master trên server khác bản đang nghe** (+2 test router thật). E2E browser `e2e/audio-range.spec.ts` trên Chrome hệ thống: GET 200 + ETag, HEAD 200, `Range: bytes=0-9` → 206 đúng 10 byte, `bytes=-8` → 206, `If-None-Match` → 304, range vượt kích thước → 416, artifact chéo chapter → 404, approve với hash cũ → 409. Còn NOT_RUN: nghe bằng tai trên master VieNeu thật (chưa có model/license) |
| A02 Preview job và màn chọn giọng | DONE (code) · live NOT_RUN | Commit `2a77367` (backend task 49 + frontend tích hợp task 53). Backend `app/modules/voices/preview.py` + migration `0017_voice_preview_jobs`: chuẩn hoá NFC+strip TRƯỚC khi đo độ dài (biên 420 hợp lệ) và trước khi tính cacheKey; cacheKey = presetId + settings hash + engine + sampleRate + text + textKind; trúng cache READY ⇒ `fromCache=true` và adapter chỉ được gọi **1 lần** (test đếm); route mới `POST /api/voices/preview-jobs` (201/400 PREVIEW_TEXT_EMPTY/400 PREVIEW_TEXT_TOO_LONG/404), `GET /{id}`, `POST /{id}/cancel` (idempotent), `POST /{id}/retry`, `GET /{id}/content` (409 khi chưa READY), `POST /preview-jobs/compare` (cùng 1 văn bản chuẩn hoá) — giữ nguyên 2 route cũ, **22 test** (`pytest backend/tests/voices backend/tests/db -q` → 48 passed, exit 0). Adapter mặc định là adapter THẬT; thiếu model ⇒ job FAILED `VOICE_MODEL_UNAVAILABLE` (không bịa audio). Frontend `VoicePreviewPanel.tsx`: văn bản mẫu/tự nhập có đếm ký tự và chặn ở client, **một player dùng chung** (playing ≠ selected), theo dõi job QUEUED→cuối, nhãn cache, Hủy/Thử lại, so sánh A/B trên cùng văn bản, giọng chưa khả dụng chỉ hiện hướng dẫn — 14 test; `VoiceScreen` gắn panel và chọn giọng đổi đúng preset dùng để render (+1 test router thật). **NOT_RUN/BLOCKED**: nghe thử bằng model VieNeu thật (chưa cài model/license) — fake chỉ chứng minh plumbing, không suy ra chất lượng giọng |
| A03 Voice plan và render segment resumable | DONE (code) · live NOT_RUN | Commit `7ac92d0` (task 55). `SpeechWorkflow.synthesize_segments()` render từng segment có checkpoint: kiểm cancel ở ranh giới → commit TRƯỚC khi gọi adapter (network ngoài transaction) → synth staging `.tmp-<id>.wav` → probe sha256 → ghi atomic qua `ArtifactStore` (os.link) → commit SAU mỗi segment; cache key nay gồm cả `narration_text` nên sửa text invalidate đúng segment; cuối lượt có retire pass chuyển artifact không dùng sang `SUPERSEDED`. Handler `SYNTHESIZE` chạy dưới worker (validate pin translationRunId/voicePlanId/revisionId) và được đăng ký trong `build_default_handlers()`; route anh em `POST /api/chapters/{id}/audio/render-jobs` (dedupe job đang chạy), route `POST .../audio/render` cũ giữ nguyên nên frontend không vỡ — 17 test mới (`pytest backend/tests/speech backend/tests/jobs -q` → 127 passed, exit 0). **Fixture 500 segment chạy thật** qua worker+handler với FakeTts, crash ở lần gọi adapter 121: `crash-after-120 took 18.6s, resume took 47.4s, adapter calls 500` — READY == 500, calls == 500 (attempts 501), status artifact == {READY}, không còn `*.partial`/`.tmp-*`. Chỉ dùng approved translation (3 test, `tts.attempts == 0` khi chưa approved); sửa text segment #2 ⇒ adapter gọi đúng 1 lần; đổi preset ⇒ cả 3 render lại; cancel 5/12 ⇒ CANCELED, READY == 5. **NOT_RUN/BLOCKED**: model VieNeu thật + mọi số đo chất lượng audio (fake chỉ chứng minh plumbing/resume) || A04 Part-master/final master/SRT | NOT_STARTED | Part 50 segment/10 phút + concat/resample/SRT chưa cài; A03 đã xong nên đủ điều kiện |
| A04 Part-master, final master và SRT | DONE (code) · FFmpeg thật NOT_RUN | Commits `4bbdfb7` + `416a39d` (task 57). Chia part ≤ **50 segment hoặc 600.000 ms** speech (ước tính, không tính pause); mỗi part là artifact `MASTER_MP3` riêng với `input_hash` dạng manifest ordered (tham chiếu tts_artifact_id + narration_sha256 + checksum từng input + pauses) và metadata `part_index`/`part_count`/`stage` ⇒ **đổi 1 segment chỉ rebuild part chứa nó**, part khác + final reuse từ cache (test đếm `processor.master_calls == 2`: 1 part + 1 final). Final concat từ các part file với manifest `part_sha256s`/`part_input_hashes`; pause ở ranh giới part được giữ đúng một lần. **Probe bắt buộc cho cả part và final trước khi ghi READY** (checksum + 44100/mono + duration > 0): checksum lệch ⇒ không artifact nào, không mất segment, render lại lành. FFmpeg fail ở final ⇒ master cũ và mọi segment vẫn READY (supersede chỉ chạy SAU khi bản mới tồn tại); master/SRT cũ → `SUPERSEDED` và **không xoá file**. **SRT dùng duration part THẬT** qua ranh giới part (cue đầu part 2 == duration part 1 thật + pause), không dùng duration ước tính. Chặn duyệt part artifact (`PART_ARTIFACT_NOT_APPROVABLE`); `RenderedChapterView` thêm `part_artifact_ids`. Giữ contract cũ: **regenerate explicit luôn tạo master mới** (`force_final`), chỉ re-render không đổi gì mới được reuse final. **Bug thật do tôi tìm và sửa (416a39d)**: `ArtifactCache.lookup` không scope theo chapter nên hai chương có nội dung giống hệt nhau (dễ xảy ra với fake processor) **dùng chung một hàng SRT** ⇒ chương thứ hai không có SRT READY của riêng nó và **export gate đổ `SRT_REQUIRED`** (2 spec E2E chỉ fail trong full suite vì phụ thuộc thứ tự). Đã thêm `_ready_chapter_artifact` + đưa run id vào SRT hash + **1 test hồi quy** (đọc: 2 chương cùng nội dung không bao giờ dùng chung artifact) — 16 test trong `test_part_master.py`. **NOT_RUN**: FFmpeg thật (binary không có trên máy — `ffmpeg -version` exit 1), loudnorm, chất lượng audio, model VieNeu |
| E01 Export bundle và review metadata | DONE (code) · **browser E2E PASS** | Commit `74dc4b7` (backend task 52 + nối UI). Backend `app/modules/exports/review.py` + route `GET /api/chapters/{id}/exports/status` (404 `CHAPTER_NOT_FOUND`): gate hiện tại + bundle READY mới nhất mỗi kind, `files` lấy thật từ namelist của zip, `verified/mismatches` đối chiếu từng file với `checksums.sha256` **nằm trong chính zip**; đủ 4 luật stale (`GATE_BLOCKED`, `RIGHTS_CHANGED`, `MASTER_CHANGED`, `TRANSLATION_CHANGED`) và fallback `UNKNOWN_*` **vẫn tính là stale** (fail-closed); không ghi DB, không mạng, không upload — 16 test mới (`pytest backend/tests/exports -q` → 25 passed, exit 0). Frontend `ExportWorkflow.tsx`: gate + hash rút gọn, validate metadata tập ở client (tiêu đề rỗng / số tập < 1 thì **không** gọi API), **publication bị disable khi gate chặn** (archive riêng tư không tự cấp quyền public), thẻ kết quả chia nhóm file (audio/transcript/metadata/provenance/license/checksum) + manifest, cảnh báo STALE kèm nút “Tạo lại bundle”, và dòng nhắc C08 “studio không tự upload” — 11 test. Nối UI: `ExportScreen` nay render `ExportWorkflow` thật và `ExportGate.tsx` cũ đã bị xoá; 3 spec E2E cũ được cập nhật theo panel mới. **Browser E2E: 11/11 passed** trên Chrome hệ thống, gồm kiểm “không có request nào ra ngoài studio”. Frontend suite **237 passed / 43 file**; `npm run build` PASS. KHÔNG có upload tự động (thao tác thủ công theo C08) |
| V01 Fixture và benchmark chạy lại | DONE (code, đo thật) | Task 48: CLI `scripts/benchmarks/run.py` đúng giao diện plan §7 (`--fixture S1|S2|C500|C2K|J10K|SSE30|AUDIO500 --seed --warmup --iterations --output`), mặc định fake/no-network + data root TẠM, guard chặn `connect/connect_ex/create_connection/getaddrinfo` ra ngoài loopback; report JSON có `fixtureHash`/`environment`/`metrics p50-p95-p99`/`queries`/`responseBytes`/`rssMb`/`walBytes`/`thresholds` và tách pha db/queue/provider/ffmpeg; **12 test** (`pytest backend/tests/scripts -q`, exit 0) gồm cả self-check deterministic và “không chạm data root thật”. Chạy thật 7 fixture, kết quả lưu tại `docs/validation/perf-*.json`: S1 p95 42,3 ms (≤150) PASS · S2 35,7 ms (≤200) PASS · C2K 20,1/40,3 ms (≤300/400) PASS · J10K claim 26,8 ms (≤150) PASS, busy 0,0 %, 0 duplicate claim · C500 RSS +8,92 MiB (≤256) PASS · AUDIO500 0 duplicate READY, 500/500 resume, 10/10 Range 206 PASS · payload event 136 B (≤16 KiB) PASS. **SSE30 replay 1k event: FAIL đã được SỬA và nay PASS** (commit `e471327`, task 54). Trước: p95 346,5 ms với **1.205 câu SQL/lượt** (SQLite chỉ ~26 ms; bottleneck ở Python/ORM: lọc cursor bằng Python + N+1 + `_sync_event_log` quét toàn bộ mỗi lượt). **Sau khi sửa**: p95 **100,2 ms**, **5 câu SQL/lượt**, 0 FAIL; 5 lần chạy lại p95 = 83,8 / 79,2 / 100,2 / 114,1 / 116,1 ms đều PASS (biên 2,6–3,8× so với ngưỡng 300 ms). Cách sửa: lọc cursor bằng SQL (`WHERE sequence_id > :after` dùng INTEGER PRIMARY KEY), bỏ N+1 bằng một câu cho mỗi loại entity, và `_sync_event_log` dùng anti-join `NOT EXISTS` thay vì quét toàn bộ; hợp đồng HTTP và tính parity/không-bỏ-sót event giữ nguyên (9 test mới, `pytest backend/tests/events -q` → 17 passed). **V02 (fe664dd — task 62)**: thêm 6 fixture đo thật (CHLIST/CHSWITCH/CANCELCKPT/EDITOR8TAB/SSE30MIN/BACKUP) sinh **17 mục ngưỡng: 13 PASS · 2 FAIL · 2 NOT_RUN** (kểm bằng code thật: router/JobRunner/Worker/RecoveryArtifactWriter/BackupService, và với phần frontend thì chạy chính module TS bằng Node). Đạt: đổi chương cached p95 24,7 ms (≤200, fetch 18,5 ms báo riêng); cancel sau checkpoint p95 **75,5 ms** (≤5 s); 8 tab evict/reopen không mất nháp (8/8 khôi phục từng byte); store SSE 760 metadata (≤1.000); backup integrity/checksum/con trỏ 25/25 + **không copy WAL riêng** (3 hàng chỉ nằm trong WAL đều vào snapshot). ~~**2 FAIL**~~ → **1 FAIL đã đóng, 1 FAIL còn lại**. (1) ~~**chapter list không lọc server-side**~~ — `q=ZZQ-MARKER` trả 100 dòng và `total=1000` bằng đúng tổng không lọc ⇒ tham số bị bỏ qua (`api/projects.py` chỉ nhận limit/cursor) — **đÃ SỬA tại commit `00148a7` (task 64) + `e20e3e5`**: lọc chạy trong SQL và áp TRƯỚC phân trang (cùng bộ filter cho câu đếm và câu trang, cursor đi trên tập đã lọc; cursor cũ vẫn dùng được), kèm **bỏ N+1** của `_chapter_summary`. Đo lại cùng fixture/seed: `overallStatus` FAIL→**PASS**; `q=ZZQ-MARKER` trả 1000/100 → **12/12**; SQL mỗi trang **302 → 4** (hằng số, không tăng theo limit); p95 **109,95 → 17,54 ms** (db 7,62 → 0,58 ms); `responseBytes` khi không lọc **y hệt từng byte** (34053/34116) ⇒ hợp đồng không đổi; frontend có ô lọc + chọn trạng thái gửi `q`/`status` lên server (15 test backend + 5 test UI). (2) ~~**Backup chưa có API incremental/progress**~~ — `BackupService.create()` không có tham số nào (kiểm bằng `inspect.signature` trên class thật). **ĐÃ SỬA tại commit `3c9f085`**: `create(*, progress=None)` phát `BackupProgress` theo phase thật (starting → database → artifacts mỗi 10 file/8 MiB → manifest → verifying → completed) và route `GET /api/storage/backups/progress` (204 khi rảnh / 200 khi đang chạy); snapshot artifact dùng **`os.link` cho file không đổi** (fallback copy), manifest ghi `linked_artifact_count`/`copied_artifact_count`/`base_backup_id`/`artifact_index[].storage`. Đo lại: **`overallStatus` FAIL → PASS (4 PASS · 0 FAIL)**; `create` p95 **5.517 → 1.805 ms**; fixture 124,1 → 60,6 s. Tự chứa được **kiểm độc lập bởi điều phối**: xoá hoàn toàn bản base rồi `verify()` bản sau vẫn `ok=True`, byte của cả 3 artifact đọc được (2 linked / 1 copied). **Giới hạn đã ghi rõ**: file DB vẫn luôn là bản sao đầy đủ qua SQLite Online Backup API — không có block-level incremental cho DB. **2 NOT_RUN có lý do cụ thể**: long task khi gõ (cần browser thật — harness chỉ có Python + Node headless) và heap 30 phút (đã **rút ngắn còn 90,1 s**, không đủ 1.800 s — không suy ra PASS). Quan sát thêm đã đo: trang 100 dòng tốn ~302 câu SQL do `_chapter_summary` chạy 3 truy vấn/chương (SQLite chỉ ~6 ms; ~110 ms là Python/ORM) — đã giao task 64 sửa cùng lúc với bộ lọc. Còn lại của V01:
| R01–R03 Migration/rollout/docs/cleanup | NOT_STARTED | — |
| X01–X07 Phase 2 extensions | NOT_STARTED | R02 chưa đạt; không bật |

## Ghi chú theo milestone

- M0 ✓ (F01–F03), M1 ✓ (S01–S03, P01–P02), M2 ✓ (P03–P06), M3 ✓ (C01–C06).
- M4: J03 DONE; J02 DONE; J01 PARTIAL (handler thật + crash coverage; REVIEW/REPAIR/SUMMARIZE cần provider/model); J04 PARTIAL (engine + SSE + delta→draft; delta sống giữa job NOT_RUN).
- M5: U01 **browser audit PASS** (responsive/1.4.4/focus/Viet-CJK, sửa overflow ngang thật); U02 shell + route tests DONE; U03 round 1 (phân trang thư viện + import review warnings); U04 DONE (code); U05 round 1–4 (reducer + API/hook + UI tab/dock + **responsive đã chạy thật**); U06 round 1–2 (editor song ngữ + QA inspector + context trace inspector); U07 round 1–2 (panel nháp job + job store/SSE dùng chung + danh sách job); U10 round 1 (storage retention/restore-copy + feature-flag guard + appearance prefs); U08/U09 PARTIAL.
- M6: A01 code-verified (live NOT_RUN); **A02 DONE (code)** — preview job bền vững + màn chọn giọng/nghe thử;
  **A05 DONE (code + browser E2E)** — Range 200/206/416/304, player dùng URL artifact, chặn phê duyệt khi master stale.
  **A03 DONE (code)** — render segment resumable (500-segment crash/resume thật: adapter calls 500, READY 500, 0 duplicate);
  **A04 DONE (code)** — part ≤50 segment/10 phút, final concat có manifest, probe bắt buộc, SRT theo duration part thật,
  kèm bản sửa bug cache artifact không scope theo chapter. **⇒ M6 đủ 5/5 task A01–A05 (code)**; mọi mục live
  (model VieNeu, nghe kiểm chất lượng, FFmpeg thật) vẫn NOT_RUN/BLOCKED.
- M7: **V01 DONE (code, đo thật)** — harness benchmark offline + 7 fixture, bảng G-PERF có kết quả từng dòng
  (SSE30 từ FAIL đã sửa thành PASS: p95 346,5 → 100,2 ms); **E01 DONE**; **V02 DONE** (6 fixture mới: 13 PASS · 1 FAIL còn lại · 2 NOT_RUN; FAIL chapter-filter đã đóng bằng 00148a7/e20e3e5); **V03 NOT_RUN/BLOCKED** (có báo cáo riêng); R01–R03 chưa thực hiện
  (R02 là cổng phát hành).
- M8: chưa thực hiện (X-task không chặn Phase 1).

## Cam kết trạng thái

- Không task nào bị đánh DONE khi chưa đủ evidence; các acceptance phụ thuộc môi trường live
  (cloud trả phí, model VieNeu cài máy, corpus có quyền, reviewer/người nghe) được ghi
  NOT_RUN/BLOCKED và feature tương ứng giữ disabled, không claim live-verified.
- **Cập nhật môi trường browser (thay thế ghi nhận cũ)**: ghi nhận trước đây "không có
  Chromium/Chrome/Edge/Firefox binary, không có cache `ms-playwright` ⇒ visual/E2E/responsive NOT_RUN" nay
  **không còn đúng**. Chrome 152 và Edge 152 có trên máy và Playwright 1.54.2 launch được chúng qua
  `channel: 'chrome'`/`'msedge'` (probe + 7 E2E thật). Vì vậy các mục visual/responsive/E2E thuộc
  fixture/fake path đã **chạy thật** (task 44 + 45) và không còn là NOT_RUN. Các mục vẫn NOT_RUN đúng quy
  tắc §2 là những mục cần môi trường live: cloud có consent/budget (REVIEW/REPAIR/summarize handler, live
  smoke, delta sống giữa job), model VieNeu + license (A02/A03 live probe/playback), corpus có quyền +
  reviewer (V03), và benchmark G-PERF (V01 chưa viết CLI/fixture).
- Toàn bộ thay đổi trong tiến trình này đều có diff/commit riêng + command/exit code như trên.

## Kết luận phiên làm việc này

Cập nhật phiên 10/09/2026 (tiếp phiên trước): **M0–M3 hoàn chỉnh**; **M4**: J01 PARTIAL (còn handler
REVIEW/REPAIR/SUMMARIZE — cần cloud), J02/J03 DONE, **J04 + SSE replay đã tối ưu: p95 346,5 → 100,2 ms
(5 câu SQL/lượt, PASS ngưỡng 300 ms — commit `e471327`)**; **M5**: U01/U02/U04/U05 DONE (U05 có browser
E2E + 2 lỗi thật đã sửa), **U03 DONE (browser E2E + lỗi hook thật ở Shell đã sửa — commit `171d2d6`)**,
U06/U07/U08/U09/U10 PARTIAL/DONE-theo-code như ghi ở bảng trên; **M6**: A01 DONE (code), **A02 DONE (code,
live NOT_RUN — commit `2a77367`)**, **A03 DONE (code, 500-segment crash/resume thật — commit `7ac92d0`)**,
**A05 DONE (code + browser E2E — commit `bf92ea8`)**, A04 chưa làm; **M7**: **V01 DONE (đo thật, 7 fixture —
commit `4e94fdd`)**, **E01 DONE (code + browser E2E — commit `74dc4b7`)**, **V03 NOT_RUN/BLOCKED có báo cáo
(`docs/validation/quality.md`, commit `18ccc80`)**; **M8**: chưa thực hiện.

Số liệu xác minh toàn cây (sau commit `6070955`): backend **813 passed** (1 warning FK-cycle có sẵn),
frontend **237 passed / 43 file**, `npm run build` PASS, ruff sạch trên mọi file đã chạm,
browser E2E **12 passed / 8 spec** trên Chrome hệ thống. Mỗi lát cắt có report riêng trong `.superpowers/sdd/`
và commit riêng: `bf92ea8`, `8444c1b`, `2a77367`, `4e94fdd`, `18ccc80`, `74dc4b7`, `e471327`, `7ac92d0`,
`171d2d6`, `6070955`.

Số liệu xác minh toàn cây (commit `300ac00`): backend **900 passed** (1 warning FK-cycle có sẵn),
frontend **267 passed / 45 file**, `npm run build` PASS, ruff sạch trên mọi file đã chạm,
browser E2E **13 passed / 9 spec** trên Chrome hệ thống. Mỗi lát cắt có report riêng trong `.superpowers/sdd/`
và commit riêng (phiên 10/09): `bf92ea8`, `8444c1b`, `2a77367`, `4e94fdd`, `18ccc80`, `74dc4b7`, `e471327`,
`7ac92d0`, `171d2d6`, `6070955`, `8611203`, `b7c0f2c`, `8f75eda`, `fecfe76`, `d1ee513`, `78b85ac`, `fe664dd`,
`00148a7`, `e20e3e5`, `4bbdfb7`, `416a39d`, `3c9f085`, `476f1ff`.

**Hai bug thật do chính các bước kiểm chứng phát hiện và đã sửa** (không phải refactor hình thức):
1. `Shell` gọi `useMatch(a) ?? useMatch(b)` nên số lượng hook đổi khi đi từ route project sang route chapter
   ⇒ React throw giữa render, **sập cả app** sau khi import (`171d2d6`).
2. `shared/api.ts` không set `Content-Type: application/json` cho body đã `JSON.stringify` ⇒ browser gửi
   `text/plain` ⇒ FastAPI 422 `INVALID_REQUEST` ⇒ **mọi thao tác lưu từ UI đều thất bại** (`8611203`).
3. (A04) `ArtifactCache.lookup` không scope theo chapter ⇒ hai chương cùng nội dung dùng chung hàng SRT ⇒ export gate
   đổ `SRT_REQUIRED` (`416a39d`).

Còn lại (giữ nguyên trạng thái, **không** đánh dấu hoàn thành): U06 (virtualize), U07 (E2E SSE thật + route
retry/cancel của job chưa tồn tại), U09 (nested project routes), R01–R03, X01–X07, cùng **mọi acceptance cần môi
trường live** — cloud trả phí có consent/budget (REVIEW/REPAIR/summarize handler thật, live smoke),
model VieNeu + license (A01 probe, A02/A03/A04 live, nghe kiểm chất lượng), FFmpeg thật (binary không có trên máy),
corpus có quyền và reviewer (V03 — xem `docs/validation/quality.md`). Các mục này ghi NOT_RUN/BLOCKED đúng quy tắc
plan §2 và không được claim là đã kiểm định. Browser visual/responsive/E2E thuộc nhánh fixture–fake **không** nằm trong nhóm NOT_RUN.
