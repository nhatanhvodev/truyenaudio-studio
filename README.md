# Truyen Audio Studio

Personal loopback-only local studio for Chinese novel translation and audio workflow experiments.

This repository currently contains the pinned scaffold for:

- `backend/`: Python 3.12 FastAPI package root `app`
- `frontend/`: Vite React SPA mounted at `<div id="root">`

Run local services on `127.0.0.1` only.
